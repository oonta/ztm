apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ztm

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - daemonset.yaml

images:
  - name: ztm-node
    newName: ztm-node
    newTag: latest
